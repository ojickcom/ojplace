import 'package:flutter/material.dart';

class BlogEditing extends StatelessWidget {
  const BlogEditing({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("블로그 편집"),
      ),
    );
  }
}
